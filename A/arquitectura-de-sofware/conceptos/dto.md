# DTO

Es un objeto simple, sin lógica de negocio, que solo contiene atributos, sus getters y setters (o propiedades públicas en lenguajes que lo permiten). Su única misión es transportar información de un lugar a otro.